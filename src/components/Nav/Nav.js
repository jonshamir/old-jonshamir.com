import React from "react";

import "./Nav.scss";

const Nav = (props) => {
  return (
    <nav className="Nav">
      <a>About</a>
      <a>Project</a>
      <a>Contact</a>
    </nav>
  );
};

export default Nav;
